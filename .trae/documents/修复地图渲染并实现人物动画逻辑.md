我将执行以下修复和功能更新计划：

### 1. 修复地图不可见问题

**原因分析**：

* 通过检查 `main_tileset.tres`，发现图块源 ID (Source ID) 为 **2**。

* `GlobalMapController.gd` 中物体层（装饰/障碍）的绘制代码硬编码使用了 `source_id = 0`，导致无法绘制任何物体。目前测试阶段确实没有配置两个物体层的图像资源，不绘制是正常现象，无需更改。

* 地面层虽然使用了地形系统，但依赖正确的地形集配置。地面层是目前测试图像资源的地方，需要正确绘制，应该进行检查。

**执行步骤**：

1. 仅检查并更改constants中地面层图块源id，其他层也可以配置，但都设为-1（目前没有美术资源）。
2. **修改** **`GlobalMapController.gd`**：

   * 将 `_set_object_cell` 方法中硬编码的 `source_id := 0` 替换为 `_C.TILE_SOURCE_ID`。

   * (可选) 为 `_set_ground_cell` 添加回退保护，确保在地形绘制失败时至少能绘制基础图块。

### 2. 实现人物动画与翻转

**需求分析**：

* 用户已配置 `AnimatedSprite2D`。

* 状态：静止 -> `idle`，移动 -> `walk`。

* 方向：默认向右，向左移动时翻转 (`flip_h`)。

**执行步骤**：

1. **修改** **`Player.gd`**：

   * 更新节点引用：将 `_sprite` 的类型从 `Sprite2D` 更改为 `AnimatedSprite2D`，并更新路径（假设为 `$Visuals/AnimatedSprite2D`，若不存在则尝试查找）。

   * 重构 `_play_movement_animation` 和 `_update_sprite_direction`：

     * 根据 `velocity.length() > 0` 判断播放 `walk` 还是 `idle`。

     * 根据 `velocity.x` 的正负值设置 `flip_h`（`velocity.x < 0` 时翻转）。

   * 移除旧的复杂方向判断（如 `walk_down`, `walk_up`），简化为左右翻转逻辑。

### 3. 验证

* 确认代码修改无语法错误。

* 提示用户运行游戏检查地图是否显示以及人物动画是否正常。

