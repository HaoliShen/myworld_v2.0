按照以下要求整理scripts文件夹。
将静态数据脚本constants，maputils，chunkdata放在一个文件夹，命名为data。
将所有autoload脚本放在一个文件夹，命名为autoload。
将核心逻辑脚本worldmanager，interactionmanager，globalmapcontroller放在一个文件夹，命名为core。
将player脚本放在entity文件夹中。
将chunklogic，mapgenerator，camerrig放在一个文件夹中，命名为components。
ui文件夹不变。
以上应该包含了所有scripts中的脚本。
如果各个脚本里有用到相关的文件路径，同步修改。
最后将旧的空文件夹删除。